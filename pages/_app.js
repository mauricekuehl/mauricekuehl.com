import "../styles/globals.scss";
import Head from "next/head";

function MauriceKuehlCom({ Component, pageProps }) {
  <Head>
    {" "}
    <link rel="icon" href="/favicon.ico" />
  </Head>;
  return <Component {...pageProps} />;
}

export default MauriceKuehlCom;
